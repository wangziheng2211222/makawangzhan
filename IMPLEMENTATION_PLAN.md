# 玛卡小镇官网实施计划

## 1. 项目目标

第一版交付一个完整、可运行的响应式首页。首页以玛卡小镇为叙事入口，通过连续滚动镜头依次介绍四位玛卡精灵及其机器人商品形态，并在旅程结束后帮助用户比较和选择适合自己的机器人。

已确认的产品方向：

- 交付范围：完整响应式首页。
- 首要目标：帮助用户认识并选择角色。
- 浏览方式：从小镇全景依次进入四个角色区域，最后回到喷泉汇合。
- 核心人群：年轻女性用户，强调情绪陪伴、角色亲和力和精致收藏玩具感。
- 视觉定位：参考“芙崽”类产品轻盈、柔软、陪伴感强的气质，但以偏蓝薰衣草紫和玛卡小镇世界观建立独立识别，不复制对方的配色、会员卡片或胶囊组件。
- 决策边界：此前已确认的 A/A/A 决策、六章顺序和首版实施范围保持不变；本次只补充紫色视觉系统、角色真值、素材重绘门槛、三段试片和滚动引擎完善方案。

核心体验路径：

> 小镇全景 -> 啾咔 -> 小恶魔 -> 机车兔 -> 屁屁 -> 小镇重聚 -> 选择伙伴

首版不包含独立产品详情页、购物车、支付、会员系统和后台 CMS。详情、购买或留资入口通过配置链接到现有业务页面；没有现成页面时，统一落到首页的选择区。

## 2. 成功标准

### 用户体验

- 用户进入第一屏即可看见玛卡小镇和四位精灵角色，理解其世界观身份是玛卡精灵、商品形态是机器人。
- 用户可以顺畅完成六章滚动旅程，也可以通过章节导航或“跳过旅程”直接进入选择区。
- 每个角色章节使用唯一内容公式：正式中英文名 + 一个原型/性格钩子 + 一句故事冲突 + 一句口头禅；故事能力和真实产品功能进入选择区并分开呈现。
- 动画结束后，用户可以并排比较四款机器人，并进入对应购买、预约或详情入口。
- 页面、poster 和视频共享同一套冷白、柔雾紫和蓝紫环境光，但四位角色的真实主色不得被统一调色覆盖。

### 技术质量

- 桌面使用原生 `16:9` 视频链，手机使用独立生成的 `9:16` 视频链。
- 相邻镜头连接处没有明显构图跳变、黑帧或倒退卡顿。
- 移动端性能目标：LCP 不高于 2.5 秒、INP 不高于 200 毫秒、CLS 不高于 0.1。
- 开启 `prefers-reduced-motion` 时不加载滚动视频，页面仍可完整浏览和转化。
- 1440x900、390x844 及手机横屏下不出现文案遮挡、裁切错误或不可点击控件。

### 商业验证

- 记录各角色章节曝光、旅程完成、跳过、角色选择和最终 CTA 点击。
- 重点观察“角色章节曝光 -> 选择伙伴 -> 业务 CTA”的转化路径。
- 上线后与静态首页进行 A/B 测试，主要指标为角色选择率和最终业务转化率；停留时长、旅程完成率作为辅助指标。

## 3. 参考项目与技术继承关系

本方案以 [`oso95/scroll-world`](https://github.com/oso95/scroll-world) 为滚动叙事技术参考，基准版本为 commit [`71cc36d3bb150248ae36a2c552f9cbf88802a79c`](https://github.com/oso95/scroll-world/tree/71cc36d3bb150248ae36a2c552f9cbf88802a79c)。它不是 Three.js/WebGL 实时三维项目，而是“AI 预渲染静帧和视频 + 浏览器滚动控制视频时间轴”的生成与播放方案，具体说明见项目的 [README](https://github.com/oso95/scroll-world/blob/71cc36d3bb150248ae36a2c552f9cbf88802a79c/README.md#L67-L93)。

### 采用的原项目架构

玛卡小镇采用原项目的**架构 B：场景 Dive-in + Connector**。该架构专门适合微缩模型、玩具和俯瞰世界：每个场景生成一段向内飞行的 `dive-in` 视频，相邻场景之间生成一段飞出并前往下一地点的 `connector` 视频。架构选择及其与真实场景架构 A 的差异见 [`SKILL.md`](https://github.com/oso95/scroll-world/blob/71cc36d3bb150248ae36a2c552f9cbf88802a79c/skills/scroll-world/SKILL.md#L369-L392)。

六个场景按以下顺序连接：

> 小镇全景 dive-in -> connector -> 啾咔 dive-in -> connector -> 小恶魔 dive-in -> connector -> 机车兔 dive-in -> connector -> 屁屁 dive-in -> connector -> 小镇重聚 dive-in

原项目的通用产物公式为：`N` 个场景 `dive-in` 视频 + `N-1` 个 `connector` 视频。玛卡小镇的 `N=6`，因此桌面 16:9 视频链为 6 个 `dive-in` + 5 个 `connector` = 11 段，手机 9:16 视频链同样为 11 段，桌面与手机总计 22 段。

### 直接继承的机制

- 将页面滚动进度映射到 `video.currentTime`，使用 `requestAnimationFrame` 平滑追踪目标进度。
- 每章使用独立 `scroll` 权重控制滚动距离，并用 `linger` 在文案出现的中段降低镜头速度。
- `connector` 起点必须使用前一段渲染视频的真实最后一帧，终点必须使用后一段视频的真实第一帧。
- 接缝处使用很短的 `crossfade` 处理模型产生的细节漂移，但不能用交叉淡化掩盖错误的首尾构图。
- 使用静态 poster 作为首屏、懒加载和视频失败回退，只加载当前及邻近片段。
- `prefers-reduced-motion` 下不加载视频；手机必须独立生成原生 9:16 视频链，不裁切桌面版本。
- 桌面编码使用 GOP 8，手机编码使用 GOP 4；iOS 首次触摸时预热静音视频，并处理底部安全区和地址栏高度变化。
- 原始浏览器实现及配置结构见 [`scrub-engine.js`](https://github.com/oso95/scroll-world/blob/71cc36d3bb150248ae36a2c552f9cbf88802a79c/skills/scroll-world/references/scrub-engine.js#L66-L101)，加载和滚动映射见同文件的 [视频加载逻辑](https://github.com/oso95/scroll-world/blob/71cc36d3bb150248ae36a2c552f9cbf88802a79c/skills/scroll-world/references/scrub-engine.js#L198-L288)。

### 玛卡官网的改造

- 将原生脚本封装为 Next.js + TypeScript 组件和 Hook，并提供事件监听、动画帧、媒体资源和对象 URL 的销毁逻辑。
- 优先使用支持 HTTP Range 的 CDN 直接寻址，Blob 仅作为部署环境不支持可寻址视频时的回退，并在释放视频后调用 `URL.revokeObjectURL`。
- 增加 `aria-current`、`inert`、`focus-visible`、键盘导航和“跳过旅程”，补齐原始示例的无障碍缺口。
- 动画后增加普通四机器人对比区，避免整站被全屏滚动锁住。
- 现有模糊角色图只作为生成参考；允许 AI 高清重绘和镜头适配，但必须以批准后的参考图、CAD、CGI 或实拍为造型真值，不能修改产品结构与标识。

### 许可边界

原项目采用 [MIT License](https://github.com/oso95/scroll-world/blob/71cc36d3bb150248ae36a2c552f9cbf88802a79c/LICENSE#L1-L20)，允许商业使用、修改和分发；复制其代码或重要部分时必须保留原版权和许可声明。MIT 许可不覆盖 Seedance、Monid、Higgsfield、Image2、蝉镜等第三方服务，也不自动授予生成图片或视频资产的商用权；各服务条款、生成资产授权、数据条款和内容责任必须分别核验。

### 当前实现差距

- Next.js、TypeScript、CSS Modules、六章静态页面、选择区和基础无障碍结构已经存在，不再按新项目从零搭建。
- 当前配置已经生成 11 个逻辑片段，但所有 `videoDesktop` 和 `videoMobile` 均为空，因此桌面 0 段、手机 0 段、connector 0 段，运行时不会创建 `<video>`。
- 当前滚动回调只通过 rAF 节流后直接设置活动视频的 `currentTime`，没有目标进度与渲染进度插值；透明度主要依赖 CSS 时间过渡，不是由滚动位置连续控制。
- 静态 poster、角色本体和章节文案没有随局部滚动进度执行景深缩放、视差、淡入和淡出，所以即使章节发生切换，体感仍接近静态幻灯片。
- 本地蝉镜代理 `http://localhost:8787/api/chanjing/video` 于 2026-07-31 检查时连接失败；正式提交前必须重新检测，并恢复状态查询与结果下载协议。

## 4. 技术方案

### 前端基础

- 基于 `/Users/wzzz/Documents/玛卡官网` 现有 Next.js App Router + TypeScript 项目继续迭代，不迁移框架或样式方案。
- 使用 CSS Modules 管理页面样式，避免为单页项目引入额外 UI 框架。
- 首页内容保持服务端可渲染；标题、角色介绍、能力证明和 CTA 均使用真实 DOM，不把关键信息烘焙进视频。
- 滚动驱动采用原生 `requestAnimationFrame`、被动 `scroll` 监听和 `video.currentTime`，按照 `scroll-world` 的机制实现，不引入实时 3D 引擎；scroll 事件只更新目标进度，rAF 使用 90ms 时间常数的指数插值持续追踪，误差低于 `0.0001` 时停止。
- crossfade 由全局滚动位置直接计算，默认重叠宽度为相邻较短片段滚动权重的 6%；重叠区同步 seek 两个相邻视频，正向、反向和快速滚动使用同一公式。
- poster 按片段局部进度执行轻量 scale/parallax，章节文案按局部进度淡入、停留和淡出；逐帧值写入 ref 与 CSS 变量，React state 只在语义章节变化时更新。
- 运行时提供明确的销毁逻辑，清理事件监听、动画帧、视频源和对象 URL，避免 SPA 导航后的内存泄漏。

### 内容数据接口

四个角色统一由配置驱动，每个角色至少包含以下字段：

```ts
type RobotProfile = {
  id: 'jiuka' | 'little-devil' | 'biker-rabbit' | 'pipi'
  name: string
  englishName: string
  tagline?: string
  archetype: string
  personalityTraits: string[]
  originStory: string
  storyAbility: string
  catchphrase: string
  likes: string[]
  fears: string[]
  relationships?: string[]
  productCapability?: string
  productProof?: string
  audience?: string
  accent: string
  productImage: string
  ctaLabel?: string
  ctaHref?: string
  availability?: 'available' | 'preorder' | 'coming-soon'
}
```

`storyAbility` 只描述角色世界观中的情绪价值，不等同于机器人硬件或软件功能。未获得确认的产品能力、参数或销售状态不得根据角色故事或外形推测；对应字段缺失时隐藏内容，不显示虚构文案或占位规格。

媒体清单使用 11 个稳定 ID，桌面和手机是同一逻辑片段的两个独立源：

```ts
type JourneySegmentId =
  | 'dive-town'
  | 'connector-town-to-jiuka'
  | 'dive-jiuka'
  | 'connector-jiuka-to-little-devil'
  | 'dive-little-devil'
  | 'connector-little-devil-to-biker-rabbit'
  | 'dive-biker-rabbit'
  | 'connector-biker-rabbit-to-pipi'
  | 'dive-pipi'
  | 'connector-pipi-to-reunion'
  | 'dive-reunion'

type JourneyMediaSegment = {
  id: JourneySegmentId
  kind: 'dive-in' | 'connector'
  chapterIndex: number
  nextChapterIndex?: number
  posterDesktop: string
  posterMobile: string
  videoDesktop?: string
  videoMobile?: string
  scrollWeight: number
  linger?: number
}
```

试片阶段允许三个对应片段仅提供桌面源；正式媒体清单必须让 11 个逻辑片段同时具备桌面和手机源，共 22 个视频文件。crossfade 不写入片段清单，运行时统一按相邻较短片段滚动权重的 6% 推导，首段和末段不产生链外 overlap；总滚动权重按原片段权重求和，overlap 只改变相邻图层混合与 seek 区间，不重复扣减滚动距离。配置加载时校验 ID、顺序、比例、缺失源和重复源，失败时保留 poster 并输出可定位的开发错误。

### 页面组成

- `SiteHeader`：品牌入口、当前章节、跳过旅程/选择伙伴按钮。
- `TownJourney`：固定全屏舞台、滚动轨道和六章进度计算。
- `SceneLayer`：静态海报、桌面/手机视频和连接视频。
- `ChapterCopy`：章节编号、角色正式中英文名、角色原型、故事冲突和口头禅；不承载未经确认的产品功能。
- `JourneyProgress`：桌面章节导航、右侧进度轨道和整体进度条。
- `RobotChooser`：动画后的四款机器人对比与选择区域。
- `Footer`：品牌、服务条款、隐私政策和业务联系方式。

### 世界观与角色设定真值

角色内容以本地终版文档 `/Users/wzzz/Downloads/玛卡星球世界观和角色设定【终版】.md` 为角色名称、世界观身份和故事事实的最高优先级来源。实施时可以修正重复词和明显录入错误，但不得自行改变设定含义。

品牌与命名规范：

- 项目名称：玛卡星球。
- App 名称：玛卡玛卡。
- 英文商标：`MAKAPLANET`。
- 域名：`www.makaplanet.com`。
- 角色正式名称：啾咔 Juka、屁屁 Pipi、机车兔 Gee-too、小恶魔 Bobby。
- 当前首页 H1 与六章路线继续使用已确认的体验名称“玛卡小镇”，世界观正文使用“玛卡星球”；终版文档没有定义二者的地理或组织关系，上线前必须由品牌方补充一句正式关系说明，在此之前不得自行将小镇定义为首都、城市或地球落脚点。
- 正文统一使用“TA”作为角色代词；展示文案统一使用正式中英文名，不混用“啾卡”“PIPI”等非规范写法。
- 故事层统一称“玛卡精灵”或“精灵伙伴”，产品参数、商品选择与业务 CTA 层称“机器人”；不得在同一句话中混用两种身份。
- 玛卡星球全称为 `Magnokyrodemibidendarium`，居民通常简称 MKDBD 或玛卡星球；“玛卡玛卡”是能立即吸引星球居民注意力的呼唤语。
- 世界观主线是玛卡精灵因母星环境变化来到地球。TA 们奇怪、随心所欲且并不完美，但真诚地爱人，也努力学习地球知识与能力以更好地陪伴用户。
- 详细天文坐标、词源和恐龙牛历史保留为扩展世界观内容，不堆叠在首屏；首页只使用“来自玛卡星球、并不完美、真诚陪伴”这一层用户可快速理解的叙事。
- 终版原文较长，滚动章节统一展示正式中英文名、一个原型/性格钩子、一句故事冲突和一句口头禅；故事能力与完整故事进入选择区的展开内容，不将整段故事覆盖在视频画面上。

## 5. 六章滚动叙事

### 第一章：进入玛卡小镇

- 以小镇俯瞰画面作为首屏产品信号，四位精灵角色围绕喷泉出现。
- H1 固定为“玛卡小镇”；辅助文案解释四位来自玛卡星球、并不完美却真诚陪伴你的精灵伙伴在这里生活。
- 镜头从高处缓慢下降，最终稳定在进入第一位角色区域的方向。

### 第二章：啾咔

- 镜头跟随闪亮碎片从小镇街道进入巫师区域或实验室，呼应啾咔因追逐布灵布灵的东西而迷路的故事。
- 角色定位为“迷糊小跟班”的反差智者：小迷糊、粘人、没头脑，却总能用天然呆化解复杂问题。
- 世界观能力是给人带来智慧；口头禅为“肿么办？”。该能力是角色设定，不作为产品功能宣称。
- 啾咔原本是玛卡星球巫师的宠物，因追逐闪亮碎片误闯实验室，后来与 Pipi 一起逃难到地球。
- 与 Pipi 是欢喜冤家；啾咔讨厌 Pipi 的过度自信，却会被其活力感染。
- 章节强调色从小镇蓝切换为啾咔的黑、金和玫红点缀。

### 第三章：小恶魔

- 使用巫师家门、药水瓶或前景遮挡完成空间转场，表现诅咒药水意外滴落后的外形变化。
- Bobby 原本是最柔软、最治愈的小绵羊；TA 误闯啾咔主人的家后，啾咔打翻诅咒药水并滴到 TA 身上，外表因此变成邪恶小恶魔，内心仍然温柔善良。
- 角色定位为“邪恶魔系外表 + 暖心治愈内核”的反差萌；世界观能力是给人带来温暖和爱。
- 口头禅为“嘿嘿嘿”，喜欢抱抱，害怕孤独。
- 章节强调色使用粉、淡紫和高饱和红色点缀。

### 第四章：机车兔

- 镜头沿首都斯卡巴加迪的巡逻路线或机车轨迹进入角色区域，利用高挑轮廓设计更明显的纵向运动。
- Gee-too 是热爱机车的巡警，因追捕时发生意外摔伤一条腿；腿部绷带是必须保留的标志性造型。
- 角色性格是运气很差、又菜又爱玩但乐于助人；TA 吸收霉运、传递好运，并总能意外解决问题。
- 口头禅为“拉ki 拉ki”；喜欢机车，梦想去大海游泳，但害怕水。
- 章节强调色使用粉色和暖白色。

### 第五章：屁屁

- 镜头进入艺术学院训练室、镜面排练区或舞台，表现 Pipi 每天对镜练习 pose 的追梦生活。
- Pipi 好奇、活力满满、极度自信，是“普信但可爱”的追梦精；逃难到地球后仍继续追梦，也会意外激励他人。
- 世界观能力是给人带来自信；口头禅为“我好美~~~~”，近期偶尔会问“我美吗？”。
- 喜欢菠萝包，最害怕脱发；与啾咔的欢喜冤家关系需要在重聚或选择区中体现。
- 章节强调色使用绿色和黄色。

### 第六章：小镇重聚

- 镜头回到喷泉广场，四位精灵角色重新出现在同一画面。
- 主文案为“选择你的玛卡伙伴”。
- 主 CTA 滚动到 `RobotChooser`，次 CTA 为重新浏览角色旅程。

## 6. 视觉与交互规则

- 小镇层使用统一的黏土微缩质感，产品层使用经造型审核的高清 AI 重绘、产品 CGI 或实拍素材；现有模糊图不直接展示。
- 两种材质通过门、遮挡物、镜子或空间变化完成过渡，不从小镇角色直接硬切到真实产品大图。
- 主品牌色使用偏蓝薰衣草紫，通过冷白背景、低对比柔雾层和少量糖果色点缀建立年轻女性向气质，不使用大面积高饱和玫紫。
- 桌面文案位于不遮挡角色的固定区域；手机文案固定在底部安全区，并保留主体上方空间。
- 首章和终章拥有更长滚动距离；四个角色章节节奏一致，避免某个产品获得非业务原因造成的额外曝光。
- 顶部导航可以直接跳到任一章节，跳转后镜头定位到章节中段。
- “跳过旅程”始终可用，点击后进入普通产品对比区，避免动画阻碍高意向用户。

### 紫色品牌色板

| 用途 | 颜色 | 使用规则 |
| --- | --- | --- |
| 页面基底 | `#F3F5FF` | 约占页面视觉面积 70%，用于冷白底色和普通内容区 |
| 柔雾紫 | `#DDD0FF` | 约占 20%，用于环境光、淡色背景和空间过渡 |
| 主蓝紫 | `#596CF5` | 用于主 CTA、进度和选中状态 |
| 强调紫 | `#8A63F6` | 与主蓝紫组成克制渐变，不作全页背景 |
| 角色点缀 | `#F4A7D8` / `#8DEBC7` / `#FFD766` | 合计不超过 10%，用于角色标识、小图标和局部反馈 |
| 主文字 | `#171923` | 用于标题和关键信息，保证淡色背景上的可读性 |
| 次文字 | `#858A99` | 仅用于辅助信息，必须通过对比度检查 |

- 蓝紫渐变只用于明确的主操作、进度和焦点，不用它填满每个区块。
- 可使用柔和半透明表面，但减少会员弹窗式的大量胶囊、悬浮卡片和超大圆角；组件形态应服务机器人品牌与滚动旅程。
- 四个角色保留各自的黑金玫红、粉红、暖白和绿黄识别，通过统一的紫色环境光联系小镇，不强行将产品本体染成紫色。
- `focus-visible` 使用主蓝紫 `#596CF5` 并增加白色隔离边，保证在冷白、粉、黄、绿和视频画面上都能达到至少 3:1 的非文本对比度。

## 7. 视觉素材生产

### 现有素材状态

- 现有小镇和四位角色素材都存在模糊、细节不足或边缘质量不足的问题，全部只作为 AI 生成/重绘的造型、颜色和构图参考。
- 像素尺寸不代表有效清晰度；即使是尺寸较大的 `啾咔.png` 和 `小恶魔.png`，也不直接作为正式产品图、首屏、poster 或视频帧。
- `玛卡小镇.png` 只用于锁定小镇布局、建筑语言和角色相对位置；正式桌面和手机场景必须分别重新生成。
- `啾咔.png`、`小恶魔.png`、`机车兔.png` 和 `屁屁.png` 只用于锁定轮廓、脸部结构、主色和标志性细节；正式使用前必须生成高清主视觉并逐角色审核。
- 当前小镇画面存在内嵌矩形底板，部分环境图已烘焙椭圆阴影，角色裁切再叠加网页阴影会产生粘贴感；重绘时必须输出无矩形底板的完整环境，以及无烘焙投影的透明角色主体，投影只由最终场景统一控制一次。
- 静态桌面场景母版至少为 2880x1620，手机母版至少为 1440x2560；透明角色母版长边至少 2048px。正式 WebP/AVIF 可以按用途派生，但不得从当前低清文件直接放大。
- 未经审核的 AI 重绘图不得进入视频生成，避免错误结构在 22 段视频中被放大。

### Image2 图片生成执行规则

- 当任务涉及 `image2`、`gpt-image-2` 或同类图片生成/编辑能力时，必须先查询并使用**执行当时**的 Codex/LLM API 配置。
- 提供方、Base URL、鉴权来源和模型可用性都可能变化，不得依赖以前记录的 `model_provider`、`base_url`，也不得假设鉴权固定来自 `~/.codex/auth.json`。
- 2026-05-14 曾成功生成 `gpt-image-2` PNG，这只能作为历史可行性记录，不能作为当前端点、模型或鉴权仍然有效的依据。
- 不得把 shell 已设置 `OPENAI_API_KEY` 作为执行前提。先检查当前 Codex/LLM 配置和鉴权状态；只有实际选择的调用路径明确要求环境变量时，才提示配置，并且不得要求用户在对话中粘贴密钥。
- 默认使用当前 Codex 提供的图片生成能力。`~/.codex/skills/.system/imagegen/scripts/image_gen.py` 只可作为当前已配置 API 的包装器，或在用户明确要求 CLI/API 脚本模式时使用；不得把脚本中的默认提供方或历史配置当作固定规则，也不得修改该脚本。
- 每次素材任务应记录本次实际使用的提供方、模型和生成参数，但不记录鉴权密钥。最终采用的项目素材必须保存进官网工作区，不能只保留在 Codex 临时生成目录。

### Seedance 视频生成接口

视频仍然是预先生成并部署的官网素材，公开首页不会在访客浏览时实时生成视频。素材制作阶段可以从本地前端或开发工具提交 Seedance 任务，固定调用关系如下：

```text
前端：POST /api/chanjing/video
本地开发代理：POST http://localhost:8787/api/chanjing/video
实际蝉镜上游：POST https://open-api.chanjing.cc/open/v1/ai_creation/task/submit
```

- 前端始终请求同源 `/api/chanjing/video`，不得直接请求蝉镜上游或在浏览器代码中暴露上游鉴权信息。
- 本地开发由 `localhost:8787` 代理转发，再由服务端请求[蝉镜任务提交接口](https://open-api.chanjing.cc/open/v1/ai_creation/task/submit)；生成路由与任务面板只用于素材生产，生产环境统一返回 404，公开首页只消费已生成的 CDN 媒体。
- 接入前必须读取蝉镜当前接口文档，确认请求 JSON、鉴权 Header、任务 ID、异步状态查询或回调方式、成功响应和错误码。当前文档只固定上述三个地址，不推测任何未提供的字段。
- Base URL 和鉴权信息只存在于服务端配置；日志需要过滤密钥、签名、原始参考图地址中的敏感查询参数。
- 提交成功后应按当前接口返回的真实任务标识跟踪任务，完成后立即下载结果到受控存储；结果 URL 可能失效，不能直接作为官网长期媒体地址。
- 状态查询统一暴露为开发期 `GET /api/chanjing/video/tasks/{taskId}`，下载动作由服务端在任务成功后执行；适配器内部字段必须按实时蝉镜协议实现，不将供应商原始响应直接传给浏览器。

### 素材生成工具

- 提供开发期任务面板 `/_dev/video-tasks`，展示片段、比例、提示词版本、参考图、状态、供应商任务 ID、尝试次数、实时价格快照、累计成本、结果和脱敏错误。
- 提供 `npm run media:generate -- --manifest <path>` 服务端 CLI，复用同一适配器，支持 `--dry-run`、自动提交、轮询、失败重试、断点恢复和下载。
- 按已确认策略自动提交选定工作流，不逐批确认；瞬时网络、限流和供应商 5xx 最多重试两次，内容审核失败和无效参数不自动重试。
- 轮询间隔优先采用蝉镜实时文档要求；文档未规定时从 10 秒开始，指数退避至最多 30 秒。每次运行写入任务清单，重启后按供应商任务 ID 恢复，不重复付费提交。
- 提交前读取当次服务价格并记录来源和时间；若没有价格 API，则使用当日官方价格页的人工快照。允许自动提交完整工作流，但实时价格、单任务估算和累计成本必须持续可见。
- 只有在本地代理无法恢复，或真实文档无法确认状态查询/结果下载契约时，自动视频生产暂停；静态页面、滚动引擎和降级路径可以继续开发。

### 生成流程

1. 按上述 Image2 规则检查当前配置，以现有模糊素材为参考重新生成小镇和四位角色的高清主视觉；通过造型审核后，再制作六章静态分镜，统一等距视角、光照、建筑材质、小镇比例和偏蓝薰衣草紫环境光。
2. 恢复本地代理并按蝉镜实时文档确认提交、状态跟踪、重试和结果下载协议；通过 CLI `--dry-run` 检查三段试片清单、实时单价和预估成本。
3. 先生成 720p、16:9 的 `dive-town` 与 `dive-jiuka`，确认从小镇俯瞰下降到跟随闪亮碎片进入啾咔区域的镜头方向。
4. 从 `dive-town` 提取真实最后一帧，从 `dive-jiuka` 提取真实第一帧，再以两帧为硬约束生成 `connector-town-to-jiuka`；不得使用原始分镜或近似帧替代真实边界帧。
5. 将三段试片编码为 H.264、720p、无音轨、faststart、GOP 8 并接入真实页面，检查向前、向后和快速滚动。
6. 试片验收要求：无黑帧、硬切、首帧停顿和倒播卡顿；角色轮廓、主色、脸部与啾咔标志性细节可识别；connector 首尾匹配相邻 dive 的真实边界。失败时只重做问题片段并更新任务清单。
7. 三段试片通过后，重新生成完整 1080p 桌面正式链；720p 试片不混入正式链。先完成 6 个 dive，再从其真实首末帧生成 5 个 connector。
8. 独立生成完整 9:16 手机链：720px 宽，先完成 6 个 dive，再生成 5 个 connector；不得将桌面链裁切作为正式手机版。
9. 每个 connector 使用相邻正式视频的真实边界帧，接缝只使用短 `crossfade` 隐藏细微材质漂移；首尾构图错误时必须重做，不能依赖淡化掩盖。
10. 对桌面与手机视频执行同一套明亮、低对比、轻紫调的色彩匹配；仅调整环境与光线，不改变机器人真实主色。
11. 从每个最终视频提取第一帧，生成与视频匹配的桌面和手机 poster，输出 CDN-ready 媒体清单。

六章架构对应每个比例 6 个场景视频和 5 个连接视频，即桌面 11 段、手机 11 段，桌面与手机总计 22 段视频。文档不保留固定成本估算；三段试片、静帧、正式 22 段和重试均按执行当日实时价格分别记录。

## 8. 视频加载与性能

- 首屏优先加载压缩后的 WebP/AVIF 小镇海报，视频在浏览器空闲或用户开始滚动后加载。
- 桌面视频编码为原生 1080p、H.264、CRF 约 20、GOP 8、无音轨、启用 faststart。
- 手机视频编码为 720px 宽、H.264、CRF 约 23、GOP 4、无音轨、启用 faststart。
- 只预加载当前段及前后相邻段；离开两段以上的视频暂停并释放可回收资源。
- 优先使用支持 HTTP Range 的对象存储/CDN 直接寻址；仅在部署环境不支持可寻址视频时启用 Blob 回退。
- 视频未完成首帧绘制前始终保留对应海报，避免 iOS 出现白屏或黑屏。
- 手机首次触摸时对已加载的静音视频执行一次播放后立即暂停，完成 Safari 解码预热。

## 9. 无障碍与降级

- `prefers-reduced-motion` 下不加载任何滚动视频，改为小镇静态首屏和四角色顺序内容。
- 所有章节跳转使用可聚焦按钮，并提供清晰的 `focus-visible` 样式。
- 当前章节导航同步 `aria-current`；不可见章节同步 `inert` 和 `aria-hidden`，避免键盘进入透明内容。
- 角色产品图片使用有意义的替代文本；纯背景和装饰海报使用空替代文本。
- 支持键盘、触控板、鼠标滚轮和触屏滚动。
- 视频加载失败时保留静态章节，不阻断后续选择和 CTA。

## 10. 产品选择区

- 四款机器人使用同等视觉面积并排展示，禁止某款因图片尺寸不同而显得更重要。
- 每款展示正式中英文名、角色定位、故事能力和口头禅；真实产品功能、适用人群、销售状态和 CTA 仅在资料确认后显示。
- 桌面为四列或 2x2 对比；手机为单列顺序内容或可访问的分页切换，不使用难以比较的无限轮播。
- 点击“选择这个伙伴”记录角色 ID、来源章节和销售状态，再进入配置的业务链接。
- 在产品资料齐全后增加简短的差异对比表；首版不制作未经确认的性格测试算法。

## 11. 埋点设计

- `town_journey_view`：小镇旅程进入视口。
- `journey_chapter_view`：某角色章节达到 50% 可见进度，携带 `robot_id`。
- `journey_skip`：用户跳过旅程，携带当前章节。
- `journey_complete`：用户到达重聚章节。
- `robot_select`：用户在章节或选择区点击某款机器人。
- `business_cta_click`：用户进入购买、预约或详情入口。

相同事件在单次页面浏览中按业务需要去重，避免滚动往返重复计算章节曝光。

## 12. 实施阶段

### 阶段一：内容与静态原型

- 基于现有 Next.js 页面完善角色配置、稳定媒体清单、紫色视觉令牌和响应式布局，不重建项目。
- 现有 PNG 只进入 AI 生成参考集，使用经造型审核的高清重绘稿完成静态小镇、四角色章节和选择区。
- 将终版世界观文档纳入内容真值来源，录入四个角色的正式名称、故事、口头禅、喜好、恐惧和关系；故事能力与真实产品功能分开建模。
- 将滚动驱动升级为目标进度插值、滚动 crossfade、poster 视差和章节文案联动，使用占位时间轴验证正向/反向滚动、跳过入口和无障碍降级。
- 增加开发期同源适配器、任务面板、CLI、任务清单、成本日志和生产 404 防护。
- 验收后冻结六章顺序、每章文案长度和镜头停留点。

### 阶段二：三段 720p 动态试片

- 运行时检查当前 Codex/LLM 图片生成配置、可用模型和鉴权来源，不复用历史端点假设。
- 启动并验证 `http://localhost:8787/api/chanjing/video`，再按蝉镜最新接口文档确认提交、状态跟踪和结果下载流程。
- 自动生成 `dive-town` 和 `dive-jiuka`，再使用两段真实边界帧生成 `connector-town-to-jiuka`。
- 将三段 720p 桌面试片接入真实页面，检查镜头方向、首尾接缝、正反滚动、文案时机、主体遮挡和紫色环境光一致性。
- 三段试片通过技术与角色造型评审后，才允许开始正式 22 段生产。

### 阶段三：正式视频与手机链

- 重新生成最终 1080p 桌面视频链 11 段和独立 9:16 手机视频链 11 段，共 22 段；不复用 720p 试片作为正式桌面片段。
- 完成编码、海报提取、CDN 上传和媒体配置。
- 逐个连接点进行截图与视觉比对，发现跳变时只重做问题片段。

### 阶段四：测试与上线

- 完成功能、响应式、性能、无障碍和低端手机测试。
- 接入业务 CTA 和正式埋点。
- 小流量发布，观察视频错误率、性能指标和转化漏斗，再扩大流量。

## 13. 测试清单

- Playwright 覆盖 1440x900、390x844、手机横屏和至少一个低高度桌面视口。
- 验证首屏、小镇到四角色、四角色之间、最后重聚的所有连接点。
- 验证向下滚动、向上滚动、快速滚动、章节跳转和跳过旅程。
- 三段试片接入后验证页面实际创建 `<video>`，滚动时 `currentTime` 连续变化；connector 首帧匹配 `dive-town` 真实末帧，末帧匹配 `dive-jiuka` 真实首帧。
- 验证目标进度插值不会在停止滚动后长期漂移；crossfade 透明度完全跟随滚动位置，反向滚动时按同一路径恢复，不依赖 CSS 计时器。
- 在 4 至 6 倍 CPU 降速下快速滚动，确认没有持续冻结、黑帧和明显输入延迟。
- 验证桌面实际加载 16:9 素材，手机实际加载 9:16 素材。
- 核对每个比例均为 6 个场景视频和 5 个连接视频，桌面 11 段、手机 11 段，总计 22 段。
- 使用 `ffprobe` 校验桌面/手机分辨率、H.264、无音轨、faststart 和 GOP 8/4，并执行黑帧检查。
- 验证 iOS 首次滚动、地址栏收起、设备旋转和安全区布局。
- 验证 `prefers-reduced-motion`、键盘导航、焦点顺序和屏幕阅读器章节状态。
- `prefers-reduced-motion` 下断言 DOM 中不存在 `<video>`，静态章节、跳过旅程和选择区仍可完整使用。
- 验证视频请求失败后海报、文案、选择区和 CTA 仍然可用。
- 验证埋点事件名称、参数和单次浏览去重。
- 验证前端只请求 `/api/chanjing/video`，浏览器包和网络请求不泄露蝉镜上游鉴权。
- 使用当前蝉镜接口契约模拟提交成功、鉴权失败、限流、任务失败、超时和结果 URL 失效；所有错误必须能定位到具体素材任务。
- 本地代理未启动时给出明确错误，不回退为浏览器直连上游。
- 验证任务面板、提交接口和状态接口在生产环境统一返回 404；客户端包、任务清单和日志均不包含鉴权信息。
- 验证 CLI 的 `--dry-run`、自动提交、最多两次瞬时错误重试、任务恢复、结果下载、实时价格快照和累计成本。
- 检查正式媒体清单不再引用现有模糊源图；每个角色的高清重绘稿通过轮廓、脸部结构、主色、标志性细节和边缘清晰度审核。
- 在 1x/2x 桌面和 2x/3x 手机截图中检查建筑、角色眼睛、毛绒边缘和小型饰件，不允许出现明显插值发虚、内嵌矩形底板或双重阴影。
- 对照终版设定核验 Juka、Pipi、Gee-too、Bobby 的名称、口头禅、喜好、恐惧、关系和故事能力，确保角色内容没有串位或被写成产品功能。
- 使用桌面和手机截图检查主基底、柔雾紫、蓝紫 CTA 和角色点缀的使用层级，页面不得呈现为单一高饱和紫色块。
- 验证正文、按钮、焦点态与半透明表面的 WCAG AA 对比度；页面、poster 和视频之间不出现明显色温跳变。

## 14. 上线前必须提供

- 终版角色文档中明显笔误修正后的品牌确认稿；角色正式中英文名、性格故事、故事能力、口头禅、喜好和恐惧已以终版文档为准，不再自行改写。
- 每款机器人的真实产品功能、功能证明和适用人群；这些信息与角色的世界观能力分开维护。
- 每款的销售状态、价格展示规则和目标 CTA 链接。
- 以现有素材为参考生成并经品牌方审核的四位角色高清主视觉；如有产品 CGI、CAD 或实拍，纳入造型真值参考。
- 品牌 Logo、正式字体、隐私政策和联系方式。
- 执行时可访问的 Codex/LLM 图片生成配置与模型权限；不在文档中固化提供方、Base URL 或鉴权路径。
- 蝉镜服务端鉴权配置、当前接口文档，以及任务状态查询或回调的完整契约。
- 图片/视频生成服务及最终 CDN/部署平台的账号与商用授权确认。

## 15. 主要风险与处理

- **世界观强于产品认知**：滚动章节负责角色原型与情绪价值，选择区独立展示经确认的真实产品功能与证明，避免两类信息混写。
- **生成画面改变产品外观**：允许 AI 对现有模糊素材进行高清重绘和镜头适配，但必须以参考图及可用的 CGI、CAD、实拍为造型真值；未经逐角色审核的结果不得进入视频链。
- **视频过重导致移动端流失**：独立手机链、邻近加载、静态海报优先和资源释放共同控制成本。
- **滚动体验阻碍高意向用户**：始终提供跳过入口，并在旅程后保留普通产品对比区。
- **连接处出现跳帧**：使用真实渲染帧锁定首尾，并在上线前逐接缝截图检查。
- **故事能力被误写成产品功能**：角色设定已确认，但真实产品功能、证明、销售状态和业务 CTA 仍须在上线前冻结并独立审核。
- **历史 Image2 配置失效**：每次执行先检查当前 Codex/LLM 配置、鉴权来源和模型可用性，不复用 2026-05-14 的端点假设。
- **蝉镜接口版本变化**：实现和测试均以当次实时接口文档为准，客户端只依赖稳定的同源代理路径。
- **同类产品视觉趋同**：只继承轻盈、柔软和陪伴感的情绪方向，用玛卡紫色环境光、小镇世界观和收藏玩具构图建立差异，不直接复制同类产品的界面组件。
- **模糊源图被误用为正式素材**：源图只进入生成参考集，正式清单只接受通过造型与清晰度审核的高清重绘稿。
