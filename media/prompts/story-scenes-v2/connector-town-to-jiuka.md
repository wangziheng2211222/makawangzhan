# 小镇到啾咔

首帧严格匹配实际编码的 `dive-town` 最后一帧，末帧严格匹配实际编码的 `dive-jiuka` 第一帧。镜头沿小镇既有街道自然前行，一枚金色闪片从前方掠过，成为唯一视觉引导；镜头跟随它穿过巫师区域的拱门，平稳进入黄昏实验室并在末帧构图处停稳。运动方向连续、速度均匀，不倒退、不突然变焦。

首尾画面中的建筑、角色和物件必须各自严格匹配真实边界帧。过渡中不得随意增加、删除、复制或融化建筑，不得生成第二个清晰角色。不要黑帧、白闪、硬切、文字、Logo、水印、强运动模糊或独立插帧风格。
